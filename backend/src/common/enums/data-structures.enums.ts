export enum DataStructureDifficulty {
  principiante = 'principiante',
  intermedio = 'intermedio',
  avanzado = 'avanzado',
}

export enum DataStructureContentType {
  general = 'general',
  property = 'property',
  operation = 'operation',
  application = 'application',
  resource = 'resource',
}

export enum ContentFormat {
  text = 'text',
  video = 'video',
  image = 'image',
  link = 'link',
}
